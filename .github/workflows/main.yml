name: BlackRock BTC Tracker

on:
  schedule:
    - cron: '0 * * * *'  # Se ejecuta al inicio de cada hora, todos los días
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Restore last date from cache
      id: cache-last-date
      uses: actions/cache@v3
      with:
        path: last_value.txt
        key: last-btc-date-v1

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run script
      id: script
      env:
        TWITTER_API_KEY: fAtUAee6ERA07Yi4thoRKRmle
        TWITTER_API_SECRET: X96zLhhEoH5a5dilOOqAk1TjWBwB14qGNBiRz49jwJThNv2oTp
        TWITTER_ACCESS_TOKEN: AIJuV5SaMZKytQB2g2blXciBuS3E4T
        TWITTER_ACCESS_TOKEN_SECRET: HuwKuloMtNujiOpZjue76kvZkYUdp9CSyiBKiW77cIKbs
        LAST_DATE: ${{ steps.cache-last-date.outputs.cache-hit == 'true' && steps.cache-last-date.outputs.cache-value || '' }}
      run: |
        # Mostrar la fecha en caché si existe
        if [ -n "$LAST_DATE" ]; then
          echo "📄 Última fecha en caché: $LAST_DATE"
        else
          echo "ℹ️ No se encontró fecha en caché, será la primera ejecución"
        fi
        
        # Ejecutar el script y capturar la salida
        echo "🚀 Ejecutando script..."
        OUTPUT=$(python main.py 2>&1)
        echo "$OUTPUT"  # Mostrar la salida
        
        # Extraer la última fecha de la salida del script
        if [[ "$OUTPUT" =~ ::set-output[[:space:]]name=LAST_DATE::(.*) ]]; then
          LAST_DATE="${BASH_REMATCH[1]}"
          echo "✅ Nueva fecha obtenida: $LAST_DATE"
          # Guardar en caché
          echo "$LAST_DATE" > date_cache.txt
          echo "should-save-cache=true" >> $GITHUB_OUTPUT
        else
          echo "ℹ️ No se generó nueva fecha para guardar en caché"
        fi

    - name: Save date to cache
      if: steps.script.outputs.should-save-cache == 'true'
      uses: actions/cache/save@v3
      with:
        path: date_cache.txt
        key: last-btc-date-v1
