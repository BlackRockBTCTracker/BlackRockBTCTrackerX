name: BlackRock BTC Tracker

on:
  schedule:
    - cron: '0 * * * *'  # Se ejecuta al inicio de cada hora, todos los días
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Get current date for cache key
      id: date
      run: echo "date=$(date +'%Y%m%d%H%M%S')" >> $GITHUB_OUTPUT

    - name: Cache last date
      id: cache-last-date
      uses: actions/cache@v3
      with:
        path: last_value.txt
        key: last-btc-date-${{ steps.date.outputs.date }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run script
      env:
        TWITTER_API_KEY: fAtUAee6ERA07Yi4thoRKRmle
        TWITTER_API_SECRET: X96zLhhEoH5a5dilOOqAk1TjWBwB14qGNBiRz49jwJThNv2oTp
        TWITTER_ACCESS_TOKEN: AIJuV5SaMZKytQB2g2blXciBuS3E4T
        TWITTER_ACCESS_TOKEN_SECRET: HuwKuloMtNujiOpZjue76kvZkYUdp9CSyiBKiW77cIKbs
      run: |
        # Mostrar estado inicial del directorio
        echo "📂 Contenido del directorio antes de ejecutar el script:"
        ls -la
        
        # Verificar si el archivo de caché existe
        if [ -f last_value.txt ]; then
          echo "✅ Valor anterior encontrado en caché"
          echo "📄 Contenido de last_value.txt: $(cat last_value.txt)"
        else
          echo "ℹ️ No se encontró valor anterior, será la primera ejecución"
        fi
        
        # Ejecutar el script
        echo "🚀 Ejecutando script..."
        python main.py
        
        # Verificar el archivo después de la ejecución
        echo "\n📂 Contenido del directorio después de ejecutar el script:"
        ls -la
        
        if [ -f last_value.txt ]; then
          echo "✅ Valor actual en last_value.txt: $(cat last_value.txt)"
          echo "📝 Guardando en caché para la próxima ejecución..."
          # Forzar la escritura del archivo con la fecha actual
          echo "$(date +'%b %d, %Y')" > last_value.txt
          echo "✅ Nuevo valor forzado en last_value.txt: $(cat last_value.txt)"
        else
          echo "❌ ERROR: No se encontró last_value.txt después de ejecutar el script"
        fi
